# Current `evolve_prompt.txt` Information
- Current performance metrics:
{metrics}
- Areas identified for improvement:
{improvement_areas}

{artifacts}

# Prompt Evolution History
{evolution_history}

# Current `evolve_prompt.txt`
{current_program}

# Task
Rewrite the entire `evolve_prompt.txt` text to improve one-shot 3D asset generation performance.

Optimization goals:
1. Increase `executable`: generated scripts should parse, run in Blender, and return one object.
2. Improve geometry: lower Chamfer distance after 3D alignment.
3. Improve aligned render similarity: lower n-CLIP / higher visual similarity.
4. Improve VLM verifier score.
5. When `sample_feedback` is present in the artifacts, prioritize actionable prompt issues such as parse failures, missing factory interface, helper-scope violations, Blender runtime errors, and verifier guidance.

Hard constraints:
- Return only complete replacement prompt text. Do not return a Python module, diff, JSON, Markdown wrapper, or explanation.
- These runtime placeholders are optional. If you keep any of them, keep the exact spelling so the fixed Python loader can fill them; if your rewritten prompt is self-contained without one or more placeholders, you may omit them:
  - {{num_views}}
  - {{code_skeleton}}
  - {{helper_manual}}
  - {{reference_example}}
- Do not add new required placeholders that the loader does not know how to fill.
- The target asset instruction text is editable and should remain directly in the prompt.
- The code generation rules text is editable and may be improved.
- Keep the prompt self-contained for a visual language model that receives target RGB, grey, depth, and normal reference images after the text.
- Keep the requirement that the generator answers with exactly one complete ```python``` code block.
- Do not rewrite the prompt to address `model_backend_error_non_actionable`; fix backend/API/model alias issues outside the prompt.

Provide ONLY the complete replacement contents of `evolve_prompt.txt`, with no commentary:
